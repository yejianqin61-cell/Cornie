# 077 M6 接口对齐阶段验证与收口

## 1. 任务目标

为 `M6 人类接口集对齐模块` 建立统一阶段验证入口，确认人类侧已经具备与模型侧对等的长期记忆 wiki 能力。

## 2. 任务来源

- `doc/design/Cornie-0627-长期记忆LLM-Wiki正式设计.md` 第 16.4 节

## 3. 前置依赖

- `074-Memory-Wiki人类只读接口接入.md` 已完成
- `075-Memory-Wiki人类写接口与确认入口.md` 已完成
- `076-Memory-Wiki高风险治理页面入口.md` 已完成

## 4. 涉及范围

- `scripts/`
- `package.json`

## 5. 当前问题

没有统一验证入口，就无法证明人类接口集已和模型工具集真正对齐。

## 6. 目标设计

本任务完成后应具备：

- `M6` 模块级验证脚本。
- 覆盖只读、写入、高风险治理入口的检查。

## 7. 实现步骤

### Step 1

整理验证范围。

### Step 2

新增统一验证脚本。

### Step 3

在 `package.json` 挂载命令。

## 8. 测试点

- 子验证脚本全部通过。
- 模块级命令可独立运行。

## 9. 完成标准

- `M6` 存在统一阶段验证入口。

## 10. 交付物

- `M6` 验证脚本
- `package.json` 命令

---

**文档结束**

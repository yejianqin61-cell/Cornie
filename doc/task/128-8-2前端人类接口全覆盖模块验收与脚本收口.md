# 128 8.2 前端人类接口全覆盖模块验收与脚本收口

## 1. 任务目标

对 `8.2` 前端人类接口全覆盖模块进行统一验收，补齐聚合验证脚本和阶段验收文档。

## 2. 任务来源

- `doc/Cornie-013-当前项目完成度复评与后续行动建议.md` 第 `8.2` 节

## 3. 前置依赖

- `123-127` 已完成

## 4. 涉及范围

- `scripts/`
- `package.json`
- `doc/acceptance/`

## 5. 当前问题

如果没有模块级统一验收入口，后续很难证明“前端人类接口全覆盖”已经完成，也无法为 `8.3` 之后的模块提供稳定基线。

## 6. 目标设计

- 增加 `8.2` 聚合验证脚本
- 串联 `123-127` 的关键校验
- 输出阶段验收文档

## 7. 实现步骤

### Step 1

新增 `8.2` 聚合验证脚本。

### Step 2

在 `package.json` 注册 `verify:8.2` 或等效入口。

### Step 3

补充 `doc/acceptance/` 下的 `8.2` 阶段验收文档。

## 8. 测试点

- `123-127` 的关键验证被统一串联
- 一键命令可执行
- 阶段收口证据完整

## 9. 完成标准

- `8.2` 存在统一验收命令
- 模块级验证通过

## 10. 交付物

- `8.2` 聚合验证脚本
- 阶段验收文档

---

**文档结束**

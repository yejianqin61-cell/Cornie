# 135 8.4 发布治理模块验收与脚本收口

## 1. 任务目标

对 `8.4 发布治理与用户引导` 模块进行统一验收，补齐聚合验证脚本和阶段验收文档。

## 2. 任务来源

- `doc/Cornie-013-当前项目完成度复评与后续行动建议.md` 第 `8.4` 节

## 3. 前置依赖

- `132-134` 已完成

## 4. 涉及范围

- `scripts/`
- `package.json`
- `doc/acceptance/`

## 5. 当前问题

如果没有模块级验收入口，就无法证明：

- DeepSeek 配置持久化已可用
- 未配置 API Key 的持续引导页已落地
- 发布提示文案已完成用户化收口

## 6. 目标设计

- 增加 `8.4` 聚合验证脚本
- 串联 `132-134` 的关键校验
- 输出阶段验收文档

## 7. 实现步骤

### Step 1

新增 `8.4` 聚合验证脚本。

### Step 2

在 `package.json` 注册 `verify:8.4` 或等效入口。

### Step 3

补充 `doc/acceptance/8.4-*.md` 阶段验收文档。

## 8. 测试点

- `132-134` 的关键验证被统一串联
- 一键命令可执行
- 阶段收口证据完整

## 9. 完成标准

- `8.4` 存在统一验收命令
- 模块级验证通过

## 10. 交付物

- `8.4` 聚合验证脚本
- 阶段验收文档

---

**文档结束**

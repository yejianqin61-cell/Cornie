# 148 api高频接口契约与错误分支覆盖补齐

## 1. 任务目标

继续补齐 `src/renderer/api.js` 的高频接口契约测试与错误分支覆盖，重点覆盖日记、聊天、类目、Memory Wiki 剩余接口，以及 `204` 返回和无文本错误回退等基础边界。

## 2. 任务来源

- `doc/Cornie-013-当前项目完成度复评与后续行动建议.md` 第 `8.5` 节
- `doc/acceptance/8.5-前端测试脚手架与回归模块阶段进展验收.md`
- `coverage/frontend/coverage-summary.json`

## 3. 前置依赖

- `138` 已完成
- `142` 已完成
- `147` 已完成

## 4. 涉及范围

- `src/renderer/api.js`
- `tests/frontend/api-contract.test.mjs`
- `doc/acceptance/8.5-前端测试脚手架与回归模块阶段进展验收.md`

## 5. 当前问题

当前 `api.js` 仍是前端测试体系中最主要的函数与分支低覆盖来源之一：

- Statements `82.97%`
- Branches `76.82%`
- Functions `73.07%`
- Lines `83.19%`

现有测试已覆盖部分 query、mutation 和错误文案，但仍缺少多组高频接口的 path / method / body 契约，以及 `204` 空响应和 `HTTP status` 回退错误分支。

## 6. 目标设计

- 补齐日记、聊天、类目、Memory Wiki 余下高频接口契约
- 覆盖 `204` 响应返回 `null`
- 覆盖后端失败但 `text()` 为空时回退到 `HTTP xxx`

## 7. 实现步骤

### Step 1

扩展 `api-contract.test.mjs`，补充只读、写入、恢复、重排、合并、关联等接口断言。

### Step 2

补齐 `204` 响应与空错误文本兜底分支测试。

### Step 3

复跑该测试、前端全量回归与覆盖率，并更新 `8.5` 阶段验收文档。

## 8. 测试点

- 日记 / 聊天 / 类目 / Memory Wiki 剩余接口的 path、query、method、body 正确
- `deleteConversation` / 删除类接口等可处理 `204`
- 后端返回空文本错误时，前端能抛出 `HTTP status`

## 9. 完成标准

- `api.js` 覆盖率较当前基线显著提升
- 新增测试稳定通过，不影响其他前端回归

## 10. 交付物

- 补充后的 API 契约测试
- 更新后的 `8.5` 阶段验收文档

---

**文档结束**

# 150 Ledger工作台低覆盖函数与错误分支补齐

## 1. 任务目标

补齐 `LedgerWorkspace.vue` 中尚未覆盖的收入分支、类目新增、筛选组合、错误恢复和表单复位路径，提升当前前端最低覆盖的大型业务页之一。

## 2. 任务来源

- `doc/Cornie-013-当前项目完成度复评与后续行动建议.md` 第 `8.5` 节
- `doc/acceptance/8.5-前端测试脚手架与回归模块阶段进展验收.md`
- `coverage/frontend/coverage-summary.json`

## 3. 前置依赖

- `141` 已完成
- `149` 已完成

## 4. 涉及范围

- `src/renderer/components/LedgerWorkspace.vue`
- `tests/frontend/ledger-workspace-async.test.mjs`
- `doc/acceptance/8.5-前端测试脚手架与回归模块阶段进展验收.md`

## 5. 当前问题

当前 `LedgerWorkspace.vue` 仍是前端低覆盖主要来源之一：

- Statements `78.12%`
- Branches `73.91%`
- Functions `73.52%`
- Lines `79.83%`

现有测试已覆盖基本读取、支出新增、编辑、删除、类型筛选和类目停用/恢复，但仍缺少：

- 收入新增分支
- 新建收入/支出类目分支
- 类目筛选组合
- 新建记录后的表单复位
- 类目保存失败或记录删除失败等错误恢复路径

## 6. 目标设计

- 补齐收入、类目和筛选的剩余主干路径
- 补齐至少一类写入失败分支，验证错误提示不会吞掉
- 覆盖表单复位和 selected entry 清空逻辑

## 7. 实现步骤

### Step 1

扩展 `ledger-workspace-async.test.mjs` 的 fetch mock，支持收入记录、新增类目、失败分支和筛选组合场景。

### Step 2

补齐收入新增、类目新增、筛选组合、删除后重置、失败提示分支测试。

### Step 3

复跑该文件测试、前端全量回归与覆盖率，并更新 `8.5` 阶段验收文档。

## 8. 测试点

- 切到 `income` 类型时会走收入新增接口
- 新增收入/支出类目分别命中不同接口
- 类型筛选 + 类目筛选会带正确 query
- 删除当前选中记录后，表单回到新建态
- 保存类目或保存记录失败时会展示可读错误

## 9. 完成标准

- `LedgerWorkspace.vue` 覆盖率较当前基线显著提升
- 新增测试稳定通过，不影响现有前端回归

## 10. 交付物

- 补充后的 Ledger 工作台测试
- 更新后的 `8.5` 阶段验收文档

---

**文档结束**

# 149 App模型恢复与引导边界分支覆盖补齐

## 1. 任务目标

补齐 `App.vue` 在 DeepSeek 引导态、模型恢复、错误提示映射和状态刷新方面的剩余边界分支测试，继续提升当前主壳层页面的大文件覆盖率。

## 2. 任务来源

- `doc/Cornie-013-当前项目完成度复评与后续行动建议.md` 第 `8.5` 节
- `doc/acceptance/8.5-前端测试脚手架与回归模块阶段进展验收.md`
- `coverage/frontend/coverage-summary.json`

## 3. 前置依赖

- `136`、`140` 已完成
- `148` 已完成

## 4. 涉及范围

- `src/renderer/App.vue`
- `tests/frontend/app-smoke.test.mjs`
- `tests/frontend/app-settings-flow.test.mjs`
- `doc/acceptance/8.5-前端测试脚手架与回归模块阶段进展验收.md`

## 5. 当前问题

当前 `App.vue` 覆盖率虽然已经不低，但作为壳层文件仍有明显边界分支未覆盖：

- Statements `86.86%`
- Branches `77.48%`
- Functions `85.36%`
- Lines `86.52%`

主要缺口集中在：

- `toFriendlySettingsError` 的多类错误映射
- `refreshModelState / checkModel` 失败恢复路径
- 配置已存在但状态异常时的引导文案与恢复提示分支
- “只重新检测”“刷新状态”等非主流程按钮

## 6. 目标设计

- 补齐引导态与恢复态的状态文案分支
- 覆盖网络失败、timeout、invalid timeout、空错误等提示映射
- 覆盖“只重新检测”“刷新状态”按钮路径

## 7. 实现步骤

### Step 1

扩展 App 设置流和 smoke 测试 mock，支持 `request_failed`、`invalid timeout`、空错误、已配置但未连通等状态。

### Step 2

补齐错误映射、恢复提示、只重新检测、刷新状态、已配置但异常引导分支测试。

### Step 3

复跑相关测试、前端全量回归与覆盖率，并更新 `8.5` 阶段验收文档。

## 8. 测试点

- `request_failed` 时显示网络排查类恢复提示
- `invalid timeout`、空错误、未知错误会映射到相应友好文案
- “只重新检测”“刷新状态”按钮能触发状态刷新
- 已配置但当前未连通时，展示“钥匙已经在了”的恢复型引导文案

## 9. 完成标准

- `App.vue` 覆盖率较当前基线显著提升
- 新增测试稳定通过，不影响现有前端回归

## 10. 交付物

- 补充后的 App 引导与恢复分支测试
- 更新后的 `8.5` 阶段验收文档

---

**文档结束**

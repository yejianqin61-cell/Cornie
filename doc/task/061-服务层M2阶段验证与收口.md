# 061 服务层 M2 阶段验证与收口

## 1. 任务目标

为 `M2 Memory Wiki 服务模块` 建立阶段验证入口，汇总基础 CRUD、元数据治理、归档恢复、页面关联能力，形成进入 `M3` 前的验收门槛。

## 2. 任务来源

- `doc/design/Cornie-0627-长期记忆LLM-Wiki正式设计.md` 第 16.4 节

## 3. 前置依赖

- `058-Memory-Wiki服务层统一入口与基础CRUD.md` 已完成
- `059-状态摘要别名重要性维护能力.md` 已完成
- `060-归档恢复与页面关联能力.md` 已完成

## 4. 涉及范围

- `scripts/`
- `package.json`

## 5. 当前问题

如果没有 `M2` 级统一验收入口，后续工具层、索引层、版本层会很难判断问题到底出在服务层还是上层模块。

## 6. 目标设计

本任务完成后应具备：

- `M2` 统一验证命令。
- 覆盖核心服务能力的阶段验收入口。

## 7. 实现步骤

### Step 1

整理 `M2` 的关键验证范围。

### Step 2

新增统一验证脚本。

### Step 3

在 `package.json` 挂载命令。

## 8. 测试点

- `M2` 子验证可串行通过。
- 阶段验证结果可作为进入下模块的凭据。

## 9. 完成标准

- `M2` 存在独立阶段验证入口。

## 10. 交付物

- `M2` 验证脚本
- `package.json` 命令

---

**文档结束**

# 073 M5 工具模块阶段验证与收口

## 1. 任务目标

为 `M5 工具集增量补全模块` 建立统一阶段验证入口，确认 Memory Wiki 相关工具已经进入统一工具体系，可供模型与后续策略层使用。

## 2. 任务来源

- `doc/design/Cornie-0627-长期记忆LLM-Wiki正式设计.md` 第 16.4 节

## 3. 前置依赖

- `070-Memory-Wiki只读工具接入.md` 已完成
- `071-Memory-Wiki写工具与高风险分层.md` 已完成
- `072-Memory-Wiki合并与主题维护工具.md` 已完成

## 4. 涉及范围

- `scripts/`
- `package.json`

## 5. 当前问题

没有 `M5` 统一验证入口，就难以证明长期记忆工具集已经真正进入模型可调用体系。

## 6. 目标设计

本任务完成后应具备：

- `M5` 模块级验证脚本。
- 串行验证只读工具、写工具、治理工具。

## 7. 实现步骤

### Step 1

整理 `M5` 验证范围。

### Step 2

新增统一验证脚本。

### Step 3

在 `package.json` 挂载命令。

## 8. 测试点

- 子验证脚本全部通过。
- 模块级命令可独立运行。

## 9. 完成标准

- `M5` 存在统一阶段验证入口。

## 10. 交付物

- `M5` 验证脚本
- `package.json` 命令

---

**文档结束**

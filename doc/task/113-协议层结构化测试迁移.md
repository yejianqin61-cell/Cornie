# 113 协议层结构化测试迁移

## 1. 任务目标

把 JSON 协议相关验证从纯 `verify:*` 迁移出一层结构化 `tests/protocol` 测试。

本任务完成后，应至少覆盖：

- reply 解析
- tool_call 解析
- JSON 修复
- 字段缺失失败分支

## 2. 任务来源

- `doc/design/Cornie-0628-工具对齐与记忆治理及测试体系设计.md` 第 6.3 A 节

## 3. 涉及范围

- `tests/protocol/`
- `electron/backend/agent/jsonProtocol.js`
- `package.json`

## 4. 当前问题

协议验证目前主要集中在 `verify-task052`，不利于日常结构化回归。

## 5. 目标设计

- 把协议测试沉淀进 `tests/protocol`
- 保留 `verify:task052` 作为专项验收

## 6. 实现步骤

### Step 1

建立 `tests/protocol` 测试文件。

### Step 2

复用现有样例覆盖关键分支。

### Step 3

补运行入口。

## 7. 测试点

- reply 解析通过
- tool_call 解析通过
- 修复重试与失败分支可断言

## 8. 完成标准

- 协议层结构化测试存在
- 关键场景覆盖

## 9. 交付物

- `tests/protocol/*`

---

**文档结束**

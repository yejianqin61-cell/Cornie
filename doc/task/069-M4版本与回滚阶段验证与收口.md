# 069 M4 版本与回滚阶段验证与收口

## 1. 任务目标

为 `M4 版本与回滚模块` 建立统一阶段验证入口，作为进入 `M5` 前的正式验收门槛。

## 2. 任务来源

- `doc/design/Cornie-0627-长期记忆LLM-Wiki正式设计.md` 第 16.4 节

## 3. 前置依赖

- `066-页面版本快照结构与写入机制.md` 已完成
- `067-版本差异比对与指定版本读取.md` 已完成
- `068-页面回滚与错误恢复能力.md` 已完成

## 4. 涉及范围

- `scripts/`
- `package.json`

## 5. 当前问题

版本模块如果没有统一验收入口，后续工具层和策略层难以把它作为可靠前提来使用。

## 6. 目标设计

本任务完成后应具备：

- `M4` 模块级验证脚本。
- 串行覆盖快照、差异、回滚能力。

## 7. 实现步骤

### Step 1

整理验证范围。

### Step 2

新增统一验证脚本。

### Step 3

在 `package.json` 挂载命令。

## 8. 测试点

- 子验证脚本全部可通过。
- 模块级命令可独立运行。

## 9. 完成标准

- `M4` 存在统一阶段验证入口。

## 10. 交付物

- `M4` 验证脚本
- `package.json` 命令

---

**文档结束**

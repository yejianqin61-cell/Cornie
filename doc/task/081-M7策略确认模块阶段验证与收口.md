# 081 M7 策略确认模块阶段验证与收口

## 1. 任务目标

为 `M7 Policy 与确认流模块` 建立统一阶段验证入口，确认 Memory Wiki 工具已正式纳入策略和确认链路。

## 2. 任务来源

- `doc/design/Cornie-0627-长期记忆LLM-Wiki正式设计.md` 第 16.4 节

## 3. 前置依赖

- `078-Memory-Wiki工具风险识别与策略接入.md` 已完成
- `079-长期记忆写入与合并确认流.md` 已完成
- `080-Memory-Wiki策略降级与兜底问询.md` 已完成

## 4. 涉及范围

- `scripts/`
- `package.json`

## 5. 当前问题

没有统一验证入口，就难以确认 Memory Wiki 已真正进入策略层。

## 6. 目标设计

本任务完成后应具备：

- `M7` 模块级验证脚本。
- 串行验证风险识别、确认流、降级兜底。

## 7. 实现步骤

### Step 1

整理验证范围。

### Step 2

新增统一验证脚本。

### Step 3

在 `package.json` 挂载命令。

## 8. 测试点

- 子验证脚本全部通过。
- 模块级命令可独立运行。

## 9. 完成标准

- `M7` 存在统一阶段验证入口。

## 10. 交付物

- `M7` 验证脚本
- `package.json` 命令

---

**文档结束**

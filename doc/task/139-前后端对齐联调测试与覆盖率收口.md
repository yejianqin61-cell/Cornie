# 139 前后端对齐联调测试与覆盖率收口

## 1. 任务目标

将前端测试从“单点页面/组件”推进到“前后端对齐联调 + 覆盖率收口”，为 `8.5` 模块形成统一验收证据。

## 2. 任务来源

- `doc/Cornie-013-当前项目完成度复评与后续行动建议.md` 第 `8.5` 节

## 3. 前置依赖

- `138` 已完成

## 4. 涉及范围

- 前端测试目录
- `scripts/`
- `package.json`
- `doc/acceptance/`

## 5. 当前问题

当前即使有前端单测，仍需要补：

- 前后端契约对齐验证
- 联调层 smoke
- 前端覆盖率统计与门禁
- `8.5` 模块级统一验收入口

## 6. 目标设计

- 增加覆盖率命令与结果校验
- 增加前端联调 smoke 或等价脚本
- 增加 `verify:8.5`
- 输出阶段验收文档

## 7. 实现步骤

### Step 1

增加覆盖率统计与读取。

### Step 2

增加前后端对齐联调脚本。

### Step 3

补充 `8.5` 聚合验证脚本与阶段验收文档。

## 8. 测试点

- 覆盖率命令可执行
- 前端主要功能链路可被联调验证
- `verify:8.5` 可一键运行

## 9. 完成标准

- `8.5` 存在统一验收命令
- 前端测试脚手架与主要回归能力已正式可用

## 10. 交付物

- 聚合验证脚本
- 阶段验收文档

---

**文档结束**

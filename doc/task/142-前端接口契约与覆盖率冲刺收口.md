# 142 前端接口契约与覆盖率冲刺收口

## 1. 任务目标

继续补齐 `api.js` 高频接口契约测试，并结合前端页面 / 组件新增回归，将 `8.5` 的覆盖率继续向 `95%+` 目标推进。

## 2. 任务来源

- `doc/Cornie-013-当前项目完成度复评与后续行动建议.md` 第 `8.5` 节
- `doc/acceptance/8.5-前端测试脚手架与回归模块阶段进展验收.md`

## 3. 前置依赖

- `140`、`141` 已完成或进入后期

## 4. 涉及范围

- `src/renderer/api.js`
- `tests/frontend/`
- `coverage/frontend/`
- `doc/acceptance/`

## 5. 当前问题

当前 `api.js` 覆盖率仍明显偏低，且整个 8.5 阶段虽然测试体系已建立，但距离最终 `95%+` 目标仍差距较大。

## 6. 目标设计

- 为高频未覆盖接口继续补契约测试
- 针对覆盖率最低的页面与函数定向加测
- 复跑前端全量测试与覆盖率
- 更新 8.5 阶段验收文档

## 7. 实现步骤

### Step 1

补 `api.js` 高频读写接口测试。

### Step 2

根据覆盖率报告定向补低覆盖页面。

### Step 3

复跑全量测试与覆盖率，并更新 `8.5` 阶段文档。

## 8. 测试点

- 高频接口 query / path / body / method 正确
- 低覆盖页面关键分支提升
- 覆盖率报告可稳定产出

## 9. 完成标准

- `8.5` 接近或达到正式覆盖率门槛

## 10. 交付物

- 补充后的接口契约测试
- 新覆盖率结果
- 更新后的阶段验收文档

---

**文档结束**

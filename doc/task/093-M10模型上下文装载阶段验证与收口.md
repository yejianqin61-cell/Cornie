# 093 M10 模型上下文装载阶段验证与收口

## 1. 任务目标

为 `M10 模型上下文装载模块` 建立统一阶段验证入口，确认长期记忆摘要注入、只读补查、上下文控长已经形成正式可依赖链路。

## 2. 任务来源

- `doc/design/Cornie-0627-长期记忆LLM-Wiki正式设计.md` 第 16.4 节

## 3. 前置依赖

- `090-长期记忆Wiki摘要注入主链替换.md` 已完成
- `091-模型只读补查工具补齐与接入.md` 已完成
- `092-上下文控长与优先级装载策略.md` 已完成

## 4. 涉及范围

- `scripts/`
- `package.json`

## 5. 当前问题

没有统一验证入口，就难以判断模型上下文装载模块是否已达到“可依赖使用”的阶段标准。

## 6. 目标设计

本任务完成后应具备：

- `M10` 模块级验证脚本。
- 串行验证摘要注入、只读补查、上下文控长。

## 7. 实现步骤

### Step 1

整理验证范围。

### Step 2

新增统一验证脚本。

### Step 3

在 `package.json` 挂载命令。

## 8. 测试点

- 子验证脚本全部通过。
- 模块级命令可独立运行。

## 9. 完成标准

- `M10` 存在统一阶段验证入口。

## 10. 交付物

- `M10` 验证脚本
- `package.json` 命令

---

**文档结束**

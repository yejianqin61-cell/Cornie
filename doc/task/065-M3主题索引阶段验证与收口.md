# 065 M3 主题索引阶段验证与收口

## 1. 任务目标

为 `M3 关键词/主题索引模块` 建立阶段验证入口，形成进入 `M4` 前的统一验收门槛。

## 2. 任务来源

- `doc/design/Cornie-0627-长期记忆LLM-Wiki正式设计.md` 第 16.4 节

## 3. 前置依赖

- `062-主题索引存储结构与基础读写.md` 已完成
- `063-日期页面来源关联与增量写入.md` 已完成
- `064-别名归一化与重复主题合并准备.md` 已完成

## 4. 涉及范围

- `scripts/`
- `package.json`

## 5. 当前问题

没有统一模块验收入口，后续版本、工具、上下文模块很难判断索引能力是否已经达到可依赖状态。

## 6. 目标设计

本任务完成后应具备：

- `M3` 统一验证脚本。
- 可串行验证索引基础读写、增量补链、归一化能力。

## 7. 实现步骤

### Step 1

整理 `M3` 验证范围。

### Step 2

新增统一验证脚本。

### Step 3

在 `package.json` 挂载命令。

## 8. 测试点

- 子验证脚本可全部通过。
- `M3` 阶段门槛可独立运行。

## 9. 完成标准

- `M3` 存在统一阶段验收入口。

## 10. 交付物

- `M3` 验证脚本
- `package.json` 命令

---

**文档结束**

# 053 编排确认流与服务层最小测试体系

## 1. 任务目标

在 `052` 基础上，继续为对话编排、确认流以及 ledger / todo / schedule 服务层建立最小自动化测试，完成 `7.4`。

## 2. 任务来源

- `doc/Cornie-004-当前模块实现度评估与下阶段开发建议.md` 第 6.2 节
- `doc/Cornie-003-当前开发进度评估与下一阶段路线图.md` 第 7.4 节

## 3. 前置依赖

- `052-协议与策略最小测试体系.md` 已完成

## 4. 涉及范围

- `electron/backend/agent/orchestrator.js`
- `electron/backend/confirm/service.js`
- `electron/backend/confirm/executor.js`
- `electron/backend/ledger/service.js`
- `electron/backend/todo/service.js`
- `electron/backend/schedule/service.js`

## 5. 当前问题

当前最危险的部分是：

- orchestrator 负责主链路但缺少测试
- confirm flow 负责高风险动作恢复执行但缺少测试
- 三个业务服务层是工具落地基础，也缺少最小测试

## 6. 目标设计

- orchestrator 测试以 mock model / mock tool result 为主
- confirm flow 至少覆盖批准、拒绝、恢复执行
- service 测试至少覆盖 CRUD 关键路径

## 7. 实现步骤

### Step 1

补服务层测试。

### Step 2

补确认流测试。

### Step 3

补编排器最小链路测试。

### Step 4

形成 `7.4` 收口文档或验证记录。

## 8. 测试点

- ledger / todo / schedule 关键 CRUD
- confirm approve / reject / execute
- orchestrator 普通 reply、tool_call、confirm、ask_back 典型路径

## 9. 完成标准

- `7.4` 设计要求中的最小测试体系已基本建立
- 后续主链路改动具备最小回归保护

## 10. 交付物

- 编排 / 确认流 / 服务层测试代码
- 测试运行结果

---

**文档结束**

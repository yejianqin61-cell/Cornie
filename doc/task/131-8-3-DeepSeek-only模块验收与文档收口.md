# 131 8.3 DeepSeek-only 模块验收与文档收口

## 1. 任务目标

对 `8.3 DeepSeek-only 收口` 模块进行统一验收，并补齐阶段验收文档。

## 2. 任务来源

- `doc/Cornie-013-当前项目完成度复评与后续行动建议.md` 第 `8.3` 节

## 3. 前置依赖

- `129-130` 已完成

## 4. 涉及范围

- `scripts/`
- `package.json`
- `doc/acceptance/`

## 5. 当前问题

如果没有模块级统一验收入口，就无法证明：

- Cornie 已正式转入 `DeepSeek-only`
- 本地模型运行时代码已被清空
- 后续 `8.4` 发布治理可以基于单模型前提继续推进

## 6. 目标设计

- 增加 `8.3` 聚合验证脚本
- 串联 `129-130` 的关键校验
- 产出阶段验收文档

## 7. 实现步骤

### Step 1

新增 `8.3` 聚合验证脚本。

### Step 2

在 `package.json` 注册 `verify:8.3` 或等效入口。

### Step 3

补充 `doc/acceptance/8.3-*.md` 阶段验收文档。

## 8. 测试点

- `129-130` 的关键验证被统一串联
- 一键命令可执行
- 阶段收口证据完整

## 9. 完成标准

- `8.3` 存在统一验收命令
- 模块级验证通过

## 10. 交付物

- `8.3` 聚合验证脚本
- 阶段验收文档

---

**文档结束**

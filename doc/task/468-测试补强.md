# 468 测试补强

## 背景

Cornie-018 D-14：`memory-wiki/service.js`（1085 行）仅 2 个顺序用例；治理/版本/巡检边界无回归测试；工具接线无集成验证（450 补）；前端 vitest 存在 28 个过时断言（契约/文案与代码漂移，实测 66 用例 28 失败）。

## 目标

1. 记忆层相关用例数 ≥ 30（后端 service/治理/版本/巡检 + 前端记忆页面组件）。
2. 前端 28 个过时用例修复（对齐当前 API 契约与文案）。
3. `npm run test:full` 全绿。

## 范围

- `tests/services/memory-wiki-service.test.mjs`（结构化回归）
- 新增：治理状态机单测（459）、巡检去重单测（460）、压缩闭环单测（461）、工具接线集成测试（450）
- `tests/frontend/`（修复过时断言）
- `tests/shared/`（harness 复用）

## 设计要求

### 1. 后端结构化回归

- service：create/update/archive/restore/merge/rollback/compress 状态机与坏页容错。
- 治理状态机单测（459 转移图）、巡检去重单测（460）、压缩闭环单测（461）。
- 工具接线集成测试（450 的 verify 纳入 test:full）。

### 2. 前端修复与补强

- 修复 28 个过时用例：API 契约（`/api/memory-wiki/pages` 路径与响应结构）、UI 文案（"需要你确认一下"等）、settings/diary/chat-history/confirm-card 断言。
- 补记忆页面组件用例：新建/编辑/删除/来源展示（配合 457）。

### 3. 验收入口

- 记忆层用例数 ≥ 30；`npm run test:full` 全绿；覆盖率报告更新（配合 `test:frontend:coverage`）。

## 验收标准

1. 记忆层相关用例数 ≥ 30。
2. 前端 vitest 全绿（修复过时断言后）。
3. `npm run test:full` 全绿；覆盖率报告可生成。

## 依赖与衔接

- 依赖：459/460/461/450（被测试对象）、457（前端组件）。
- 上游设计：Cornie-018 I-15（Phase 4）；Cornie-019 §5.2（测试漂移实测证据）。

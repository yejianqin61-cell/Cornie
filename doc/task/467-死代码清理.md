# 467 死代码清理

## 背景

Cornie-018 D-13：4 个 link 服务零引用（`observation/wikiLink.js`、`observation/topicLink.js`、`chatlog/memoryLink.js`、`chatlog/topicLink.js`）；`chatlog/repository.js` 的 `createBetterSqlite3ChatlogRepositorySkeleton` 死骨架；旧 `memory/` 模块运行时零引用（随 470 退役）。

## 目标

1. 删除零引用文件与死骨架。
2. 清理后 grep 零引用 + 全量测试通过。

## 范围

- 删除：`observation/wikiLink.js`、`observation/topicLink.js`、`chatlog/memoryLink.js`、`chatlog/topicLink.js`
- `chatlog/repository.js`（删除死骨架函数）
- 相关导入清理（grep 复核后删）

## 设计要求

1. 删除前 grep 全量复核零引用（含动态引用/字符串引用）。
2. 按模块删除，每步跑相关测试。
3. 清理后 `npm run test:full` 全绿。

## 验收标准

1. grep 上述文件/函数全仓库零引用。
2. 全量测试通过（前端 vitest + 后端 fast/integration）。

## 依赖与衔接

- 依赖：446（wikiUpgrade 相关清理先行确认）。
- 上游设计：Cornie-018 I-14（Phase 4）。

# 类目映射遗留问题清单

## 1. 文档目的

本清单用于记录 `7.2` 阶段收口后仍然存在、但不阻塞当前继续进入 `7.3` 的问题与风险。

记录日期：

- 2026-06-27

## 2. 遗留问题分级规则

- `P0`：进入下一阶段前必须解决，否则会直接影响主链路可信度
- `P1`：不阻塞继续开发，但应尽快补齐
- `P2`：优化项，可在后续阶段排期

## 3. 当前遗留问题

| 编号 | 优先级 | 类型 | 问题 | 当前判断 | 建议去向 |
| --- | --- | --- | --- | --- | --- |
| `CAT-001` | P1 | 联调证据 | 前端真实确认卡点击、拒绝、结果展示的完整手测记录未沉淀 | 不阻塞后端继续开发，但不宜直接当成发布级完成 | 进入 `7.3` 前补手测记录 |
| `CAT-002` | P1 | 模型波动 | 当前主要证据来自本地 stub + 策略层验证，真实 DeepSeek 输出波动下的专项样本还不够多 | 工程正确性已较强，但真实语义边界仍需观察 | 后续结合真实对话补样本回灌 |
| `CAT-003` | P1 | 追问覆盖 | `todo/schedule` 的 `ask_back` 自动化覆盖仍偏弱，当前更强的是 `ledger` 场景 | 不阻塞主链路，但建议补专项 case | 后续可在 `task037` 方向继续补样本与 case |
| `CAT-004` | P2 | 资产组织 | 当前回归脚本仍集中在单文件，规模继续增长后可维护性会下降 | 当前还能维护，但后续会变重 | 后续可拆 `scripts/fixtures/category-flow/` |
| `CAT-005` | P2 | 发布治理 | 发布前用户引导、配置检查、云模型说明尚未进入真正实现阶段 | 不属于 7.2 阻塞项 | 进入配置与发布治理阶段处理 |

## 4. 非问题但需要持续观察的点

以下项目当前不判定为缺陷，但建议持续观察：

- 三域补查轮控目前采用“单域一次”策略，后续若业务复杂度继续提升，需要继续验证是否足够
- 类目映射日志字段已统一，但后续若再扩更多业务域，需要保持字段语义稳定
- 当前回归脚本已经达到 `18/18`，后续继续扩用例时要防止输出质量下降

## 5. 对下一阶段的约束建议

在 `7.3` 阶段继续开发时，建议遵守：

1. 若新增工具动作涉及类目映射，不要新造一套确认或补查规则
2. 若发现真实模型输出与脚本假设不一致，优先补样本、补回归，而不是直接放宽规则
3. 若前端联调发现“展示正确但后端未真正恢复执行”，应立即回流为 `P0`

## 6. 当前结论

当前遗留问题中：

- 无明确 `P0` 阻塞项
- 存在若干 `P1` 级联调与真实模型验证缺口

因此本清单支持的总体判断是：

`可以继续进入 7.3，但要把 7.2 的回归资产视为强约束，并在后续阶段补齐前端真实联调与真实模型样本验证。`

---

**文档结束**

# 类目映射全域联调记录

## 1. 文档信息

| 字段 | 内容 |
| --- | --- |
| 文档名称 | 类目映射全域联调记录 |
| 适用阶段 | `7.2 类目快照注入与映射闭环` |
| 记录日期 | 2026-06-27 |
| 记录范围 | `ledger / todo / schedule` |
| 关联任务 | `task015-task034` |
| 关联脚本 | `npm.cmd run verify:category` |

## 2. 联调目标

确认以下能力已经在三域形成稳定闭环：

- 首轮类目快照注入
- 命中既有类目并直接执行
- 只读补查后继续决策
- 新增类目确认并恢复执行
- 拒绝新增类目后的稳定收口
- 类目名模糊或信息不足时降级 `ask_back`
- 回归资产可重复执行

## 3. 联调方式

### 3.1 自动化验证

执行命令：

```powershell
npm.cmd run verify:category
```

本轮结果：

- 执行日期：2026-06-27
- 结果：`18/18` 用例通过
- 覆盖域：`ledger / todo / schedule`

### 3.2 文档与资产复核

已复核资产：

- `doc/test/类目映射闭环回归用例.md`
- `doc/test/类目映射验收清单.md`
- `doc/test/待办与日程类目映射回归补充.md`
- `scripts/verify-category-flow.mjs`

### 3.3 手工联调状态

当前结论：

- 代码侧与脚本侧证据已具备
- 前端真实点击确认卡、真实 DeepSeek 输出波动下的专项手测，本轮未执行

因此本记录中的“联调完成度”定义为：

- 后端链路：已验证
- 自动化回归：已验证
- 前端真实交互：待补最终手测

## 4. 三域联调结果

### 4.1 收支域 `ledger`

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| 首轮命中既有类目 | 通过 | `TC-001` |
| 新增类目确认并恢复执行 | 通过 | `TC-005` |
| 重复类目复用 | 通过 | `TC-009` |
| 拒绝新增后不落库 | 通过 | `TC-008` |
| 类目名模糊降级追问 | 通过 | `TC-010` |
| 只读补查上下文抽取 | 通过 | `TC-012` |

结论：

- 收支域链路最完整，已形成稳定基线

### 4.2 待办域 `todo`

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| 首轮命中既有类目 | 通过 | `TC-003` |
| 更新时重映射类目 | 通过 | `TC-029` |
| 新增类目确认并恢复执行 | 通过 | `TC-031 todo create` |
| 更新类目确认并恢复执行 | 通过 | `TC-031 todo update` |
| 拒绝新增后不落库 | 通过 | `TC-031 todo reject` |
| 只读补查单域轮控 | 通过 | `TC-032 todo` |

结论：

- 待办域主链路、确认恢复链路与收支域已基本对齐

### 4.3 日程域 `schedule`

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| 首轮命中既有类目 | 通过 | `TC-004` |
| 更新时重映射类目 | 通过 | `TC-030` |
| 新增类目确认并恢复执行 | 通过 | `TC-031 schedule create` |
| 更新类目确认并恢复执行 | 通过 | `TC-031 schedule update` |
| 拒绝新增后不落库 | 通过 | `TC-031 schedule reject` |
| 只读补查单域轮控 | 通过 | `TC-032 schedule` |

结论：

- 日程域主链路、确认恢复链路与待办域已基本对齐

## 5. 跨域一致性检查

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| 三域都支持既有类目命中 | 通过 | `TC-001/003/004` |
| 三域都支持新增类目确认恢复 | 通过 | `TC-005/006/007` 的自动化落地版本已具备 |
| 三域都支持拒绝新增后收口 | 通过 | `ledger/todo/schedule` 均有拒绝用例 |
| 三域都支持 `ask_back` 兜底 | 基本通过 | `ledger` 已自动化，`todo/schedule` 仍建议补真实联调观察 |
| 三域只读补查轮控语义一致 | 通过 | `TC-032` |
| 审计日志字段可跨域统一阅读 | 通过 | 已带 `domain / reason / candidates` 等关键字段 |

## 6. 回归资产状态

| 资产 | 状态 | 说明 |
| --- | --- | --- |
| 回归用例文档 | 完成 | 已覆盖三域与 `TC-032` |
| 验收清单 | 完成 | 已纳入单域补查限制与日志结构检查 |
| 待办/日程专项补充 | 完成 | 已形成单独文档 |
| 自动化脚本 | 完成 | 当前 `18/18` 通过 |
| 失败定位输出 | 完成 | `detail` 已带 `domain / scenario` |

## 7. 当前阶段结论

### 7.1 对 `7.2` 的判断

当前更准确的阶段判断是：

`7.2 在后端代码、策略链路、回归资产和自动化验证层面已基本完成，可视为“代码侧收口完成”。`

但仍保留一个现实边界：

- 前端真实交互与真实模型波动下的最终手测证据，本轮未补齐

因此不建议把 `7.2` 解释成“发布级完全验收完成”，更适合解释成：

`可进入 7.3 的工程收口完成状态`

### 7.2 是否建议进入 `7.3`

建议：

- 可以进入 `7.3 工具层能力对齐` 的开发

前提：

- 保留 `7.2` 当前回归资产作为后续变更的准入门槛
- 在真正发布前补一轮前端手测与真实模型手测

## 8. 建议的进入条件

进入 `7.3` 前建议遵守以下约束：

1. 每次改动策略层、确认流、类目映射或编排层，都先跑 `npm.cmd run verify:category`
2. 新增业务动作若涉及类目映射，必须复用当前三域统一语义
3. 不允许为了扩功能绕开当前确认流、补查轮控或审计日志

---

**文档结束**

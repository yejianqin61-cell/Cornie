# 7.3 测试体系模块阶段验收

## 1. 文档目的

本文档用于证明 `7.3 测试体系模块` 已完成本阶段后端收口，并形成统一的结构化测试入口、聚合验收入口与阶段验收证据。

## 2. 本阶段目标回顾

根据 [Cornie-0628-工具对齐与记忆治理及测试体系设计.md](/C:/Users/USER/Desktop/Cornie/Cornie/doc/design/Cornie-0628-工具对齐与记忆治理及测试体系设计.md:654)，`7.3` 的目标是：

- 建立 `tests/` 分层结构
- 建立 `test:fast / test:integration / test:full`
- 将 `protocol / policy / services / tools / orchestrator / memory-governance` 沉淀为结构化测试
- 保留既有 `verify:*`，形成双轨验证体系
- 提供模块级统一聚合验收入口

## 3. 本阶段完成情况

### 3.1 任务完成情况

| Task | 结果 | 说明 |
| --- | --- | --- |
| `112` tests 目录骨架与运行脚本分层落地 | 已完成 | 建立 `tests/` 分层目录与 `test:*` 运行入口 |
| `113` 协议层结构化测试迁移 | 已完成 | 落地 `tests/protocol` |
| `114` policy 层结构化测试迁移 | 已完成 | 落地 `tests/policy` |
| `115` service 层结构化测试迁移 | 已完成 | 落地 `tests/services` |
| `116` tools 与 gateway 结构化测试迁移 | 已完成 | 落地 `tests/tools` |
| `117` orchestrator 结构化测试迁移 | 已完成 | 落地 `tests/orchestrator` |
| `118` memory-governance 结构化测试迁移 | 已完成 | 落地 `tests/memory-governance` |
| `119` 7.3 测试体系模块验收与脚本收口 | 已完成 | 新增统一聚合验收入口与阶段验收文档 |

### 3.2 已落地测试分层

#### `tests/protocol`

- reply / tool_call 解析
- 包裹 JSON 提取
- 字段缺失失败分支
- JSON repair prompt

#### `tests/policy`

- allow
- ask_back
- deny
- confirm
- memory wiki 治理 confirm

#### `tests/services`

- ledger / todo / schedule 生命周期
- confirm service 生命周期
- memory wiki 生命周期
- topic index 生命周期

#### `tests/tools`

- registry 注册与读取
- gateway 分发
- tool_not_found 封装
- handler 抛错封装
- 风险等级读取与推断

#### `tests/orchestrator`

- 纯 reply 链路
- ask_back 链路
- confirm 链路
- tool followup 链路
- 只读补查链路
- 脏 JSON 解析与 repair retry

#### `tests/memory-governance`

- 合并来源迁移
- 压缩与回滚一致性
- inactive / archived / active 生命周期
- 热度衰减排序
- 巡检候选入池

## 4. 运行入口收口结果

### 4.1 分层入口

| 命令 | 结果 | 说明 |
| --- | --- | --- |
| `npm.cmd run test:fast` | 通过 | 运行 protocol / policy / services |
| `npm.cmd run test:integration` | 通过 | 运行 tools / orchestrator / memory-governance |
| `npm.cmd run test:full` | 通过 | 运行全部结构化测试，并串联 `verify:7.1 / verify:7.2` |

### 4.2 7.3 模块级入口

| 命令 | 结果 | 说明 |
| --- | --- | --- |
| `npm.cmd run verify:7.3-tests` | 通过 | 串联 `verify:task112-118` |
| `npm.cmd run verify:7.3` | 通过 | 作为兼容入口，指向同一聚合脚本 |
| `npm.cmd run verify:task119` | 通过 | 校验聚合脚本、验收文档、`test:full` 与 7.3 模块收口 |

## 5. 产出物清单

### 5.1 文档

- `doc/task/112-119*.md`
- `doc/acceptance/7.3-测试体系模块阶段验收.md`

### 5.2 脚本

- `scripts/run-tests.mjs`
- `scripts/verify-task112-test-structure.mjs`
- `scripts/verify-task113-protocol-structured-tests.mjs`
- `scripts/verify-task114-policy-structured-tests.mjs`
- `scripts/verify-task115-service-structured-tests.mjs`
- `scripts/verify-task116-tools-gateway-structured-tests.mjs`
- `scripts/verify-task117-orchestrator-structured-tests.mjs`
- `scripts/verify-task118-memory-governance-structured-tests.mjs`
- `scripts/verify-task119-7.3-test-system.mjs`
- `scripts/verify-7.3.mjs`

## 6. 阶段结论

结论：

`7.3 测试体系模块后端范围已完成。`

判断依据：

- `112-119` 已全部落地
- `tests/*` 六层结构化测试已齐备
- `test:fast / test:integration / test:full` 已形成稳定入口
- `verify:*` 与 `tests/*` 已形成双轨并存
- `verify:7.3-tests / verify:7.3 / verify:task119` 已形成模块级统一验收链路

因此项目可以从 `7.3` 正式转入后续阶段的持续开发与回归维护。

---

**文档结束**

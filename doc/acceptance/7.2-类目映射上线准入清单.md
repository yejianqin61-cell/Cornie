# 7.2 类目映射上线准入清单

## 1. 文档目的

用于判断 `Cornie-003` 第 `7.2` 阶段是否达到“可以收口、可以进入下一优先级”的准入状态。

## 2. 准入项清单

| 检查项 | 验证方式 | 当前状态 | 说明 |
| --- | --- | --- | --- |
| 收支类目首轮命中链路可用 | `npm.cmd run verify:category` 中 `TC-001` | 已完成 | 已验证 direct hit |
| 收支新增类目确认恢复链路可用 | `verify:category` 中 `TC-005` | 已完成 | 已验证确认新增并恢复原动作 |
| 收支拒绝新增时不误写 | `verify:category` 中 `TC-008` | 已完成 | 已验证拒绝后不落库 |
| 收支类目追问降级可用 | `verify:category` 中 `TC-010` | 已完成 | 已验证 vague name ask_back |
| 收支只读补查链路可用 | `verify:category` 中 `TC-012` | 已完成 | 已验证 lookup 工具返回结构 |
| 待办主链路类目映射可用 | `verify:category` 中 `TC-003`、`TC-029` | 已完成 | 已覆盖 create / update |
| 日程主链路类目映射可用 | `verify:category` 中 `TC-004`、`TC-030` | 已完成 | 已覆盖 create / update |
| 待办新增类目确认恢复闭环可用 | `verify:category` 中 `TC-031` 对应 todo case | 已完成 | 已覆盖 create / update / reject |
| 日程新增类目确认恢复闭环可用 | `verify:category` 中 `TC-031` 对应 schedule case | 已完成 | 已覆盖 create / update / reject |
| 补查轮次限制可用 | `verify:category` 中 `TC-032` | 已完成 | 已验证按域限制只读补查轮次 |
| 单轮补查缓存复用可用 | `verify:category` 中 `TC-035` | 已完成 | 已覆盖 ledger / todo / schedule |
| 域注册表配置化可用 | `verify:category` 中 `TC-036` | 已完成 | 已验证缺字段报错 |
| 审计样本导出可用 | `verify:category` 中 `TC-037` | 已完成 | 已生成样本与回灌资产 |
| 类目脏数据巡检与修复工具可用 | `verify:category` 中 `TC-038` | 已完成 | 已验证巡检、dry-run、apply |
| 性能 profiling 可运行 | `npm.cmd run profile:category` | 已完成 | 已拿到四类场景基线 |
| 性能基线文档已留痕 | `doc/test/类目映射性能基线记录.md` | 已完成 | 已回填第一轮数据 |

## 3. 准入结论

当前 `7.2` 所定义的类目映射主闭环、确认恢复、补查、缓存、审计、修复、回归和性能基线，均已具备可验证产出。

结论：

`允许将 7.2 判定为阶段完成，并转入 7.3。`

## 4. 进入下一阶段前的提醒

1. `7.2` 已完成，不代表工具层能力已全量对齐前端。
2. `7.3` 开始前，应优先复用现有类目映射底座，不要绕开确认流和补查链路另写一套。
3. profiling 已显示 prompt 体积仍是主要成本，后续扩工具时要注意继续控制上下文膨胀。

---

**文档结束**

# Cornie GitHub 前端 Issue 对照结论文档

日期：2026-07-01

## 目的

对 GitHub 当前仍处于开启状态的前端相关 issue 进行一次基于代码现状的逐条对照，明确：

1. 哪些 issue 已经被代码覆盖，但尚未关闭。
2. 哪些 issue 仍未完全落地。
3. 本轮实际补做了哪一条。

## 对照范围

当前仓库仍处于 `OPEN` 状态的相关 issue：

1. `#11` 记账页面
2. `#13` 收支页面
3. `#14` 前端-日程页面
4. `#15` 前端-日记页面

## 逐条结论

### #11 记账页面

issue 内容：

> 新建类目工具似乎缺失

对照结论：

已被当前代码覆盖，但 issue 尚未关闭。

现状说明：

1. 记账首页快捷录入区已经提供“新建类目”按钮。
2. 收支工作台也已经提供完整的类目新增、编辑、启停入口。

涉及文件：

- `src/renderer/components/LedgerHome.vue`
- `src/renderer/components/LedgerWorkspace.vue`

建议：

1. 若实际运行中入口可见且可用，可直接关闭 issue。
2. 若用户反馈的是“入口存在但不好找”，应另开可用性 issue，而不是继续沿用本条。

### #13 收支页面

issue 内容：

> 折线统计图，要有个第一象限啊，xy轴啊。

对照结论：

已被当前代码覆盖，但 issue 尚未关闭。

现状说明：

1. 收支趋势图已采用 SVG 绘制。
2. 已包含横轴、纵轴、刻度标签、网格线与图例。
3. 视觉是否满意可以继续优化，但“没有坐标轴”这一问题已不成立。

涉及文件：

- `src/renderer/components/LedgerHome.vue`

建议：

1. 如需继续美化图表，应新开“图表视觉优化” issue。
2. 当前 issue 可按“功能已补齐”进行关闭。

### #14 前端-日程页面

issue 内容：

> 当前查看、回到今天、清除筛选、新增安排这两个卡片可以缩短，放到同一行，避免罗列堆叠

对照结论：

本轮已修复。

修复说明：

1. 将“当前查看”与“新增安排”改为两个独立卡片。
2. 当新增表单展开时，桌面端改为并排双栏展示，不再上下堆叠。
3. 在窄屏下自动回落为单列，避免内容挤压。
4. 补充了更短的查看提示文案，减少顶部区块的视觉长度。

涉及文件：

- `src/renderer/components/ScheduleHome.vue`
- `doc/task/441-日程首页控制区并排紧凑化修复.md`

建议：

1. 启动前端后手动验收桌面端并排效果。
2. 若视觉方向认可，可关闭该 issue。

### #15 前端-日记页面

issue 内容：

> 铃湾日记部分，要支持 md 渲染。因为大模型返回的是我看到有##，**符号

对照结论：

已被当前代码覆盖，但 issue 尚未关闭。

现状说明：

1. 当前已经存在铃湾日记专用 Markdown 渲染组件。
2. 日记首页、铃湾日记回顾页、往年今日页都已接入该渲染组件。
3. 标题、加粗、引用、列表等基础语法已有支持。

涉及文件：

- `src/renderer/components/CornieDiaryMarkdown.vue`
- `src/renderer/components/DiaryHome.vue`
- `src/renderer/components/CornieDiaryReview.vue`
- `src/renderer/components/OnThisDayPage.vue`

建议：

1. 若当前渲染效果满足预期，可关闭 issue。
2. 若后续需要更完整的 Markdown 能力，应另开“日记 Markdown 渲染增强” issue。

## 总结

当前这 4 条 open issue 中：

1. `#11` 已覆盖，待关闭。
2. `#13` 已覆盖，待关闭。
3. `#14` 本轮已修复，待验收后关闭。
4. `#15` 已覆盖，待关闭。

## 额外说明

本轮同时修复过一条长期记忆 wiki 的运行故障链，但该问题不属于上述 4 条 GitHub issue 范围。因此，后续推进应继续以 issue 清单为主，避免开发内容与 issue 范围再次偏离。

export function registerMemoryTools(store, { registerTool }) {
  void store
  void registerTool
  // legacy memory_entries is kept only as a data compatibility layer.
  // Runtime long-term-memory tools must exclusively use
  // memory_wiki / memory_index / memory_governance.
}
